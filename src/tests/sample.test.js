const expect = require("chai").expect;
const axios = require("axios").default;
const constants = require("../utils/constants");

const routeUrl = constants.localServerUrl + "/sample";

describe("Sample", () => {
  it("test a sample endpoint", async () => {
      
  });
});
